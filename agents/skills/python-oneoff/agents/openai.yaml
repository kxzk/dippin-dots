interface:
  display_name: "Python One-Off"
  short_description: "Run one-off Python scripts with uv and PEP 723."
  icon_small: "./assets/small-logo.svg"
  icon_large: "./assets/large-logo.png"
  default_prompt: "Use $python-oneoff to write and run a throwaway Python script with inline dependencies via uv."

policy:
  allow_implicit_invocation: false

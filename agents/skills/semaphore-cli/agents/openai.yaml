interface:
  display_name: "Semaphore CLI"
  short_description: "Operate Semaphore via sem CLI workflows and debugging."
  default_prompt: "Use $semaphore-cli to run, inspect, and troubleshoot Semaphore resources with sem."
policy:
  allow_implicit_invocation: false

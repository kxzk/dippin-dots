interface:
  display_name: "Commit Policy"
  short_description: "Apply branch-aware commit message rules."
  icon_small: "./assets/small-logo.svg"
  icon_large: "./assets/large-logo.png"
  default_prompt: "Use $commit whenever a commit is requested or any workflow step performs a commit."

policy:
  allow_implicit_invocation: true

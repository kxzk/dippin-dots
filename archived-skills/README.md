# Archived skills

These skills are retained for historical reference. Active agent clients do
not load skills from this directory.

| Archived skill | Active replacement |
| --- | --- |
| `staging-kube-connect` | `spx-staging-connect` |
| `staging-server-promote` | `spx-staging-deploy` |

The client subdirectories preserve the former Codex, Claude, and Cursor skill
layouts. Do not copy an archived skill back into an active skill directory.
Update the SPX command or active `spx` skill when behavior changes.
